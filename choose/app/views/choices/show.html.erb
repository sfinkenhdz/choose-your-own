
<div>
<%= @choice.text %>
</div>

<div>
<%= @choice.resulting_outcome.name %>

<%= @choice.resulting_outcome.text %>
</div>

<div>
  <% @choice.resulting_outcome.next_choices.each do |choice| %>
    <button class="choice"><%= link_to choice.name, choice_path(choice) %></button>
    <% end %>
</div>
